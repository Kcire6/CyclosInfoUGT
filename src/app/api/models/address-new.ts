/* tslint:disable */
import { AddressManage } from './address-manage';

/**
 * Fields for a new address. The actually used and required fields depend on the user configuration.
 */
export interface AddressNew extends AddressManage {
}
