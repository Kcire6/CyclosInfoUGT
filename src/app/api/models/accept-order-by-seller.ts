/* tslint:disable */
import { BaseOrderAction } from './base-order-action';

/**
 * Parameters used to accept an order by the seller.
 */
export interface AcceptOrderBySeller extends BaseOrderAction {
}
