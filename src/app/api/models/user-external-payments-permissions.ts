/* tslint:disable */

/**
 * Permissions for external payments over a user
 */
export interface UserExternalPaymentsPermissions {

  /**
   * Can the authenticated administrator or broker view external payments of the given user?
   */
  view?: boolean;
}
