/* tslint:disable */

/**
 * Permissions for authorized payments over a user
 */
export interface UserAuthorizedPaymentsPermissions {

  /**
   * Can the authenticated administrator or broker view authorized payments of the given user?
   */
  view?: boolean;
}
