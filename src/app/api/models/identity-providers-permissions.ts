/* tslint:disable */

/**
 * Permissions over the logged user's identity provider links
 */
export interface IdentityProvidersPermissions {

  /**
   * Is the identity providers functionality enabled for the logged user?
   */
  enabled?: boolean;
}
