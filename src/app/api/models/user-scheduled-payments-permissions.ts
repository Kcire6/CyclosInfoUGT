/* tslint:disable */

/**
 * Permissions for scheduled payments over a user
 */
export interface UserScheduledPaymentsPermissions {

  /**
   * Can the authenticated administrator or broker view scheduled payments of the given user?
   */
  view?: boolean;
}
