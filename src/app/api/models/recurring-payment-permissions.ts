/* tslint:disable */

/**
 * Permissions the user has over a recurring payment
 */
export interface RecurringPaymentPermissions {

  /**
   * Can cancel the recurring payment?
   */
  cancel?: boolean;
}
