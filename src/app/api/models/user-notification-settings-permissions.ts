/* tslint:disable */

/**
 * Permissions regarding the notification settings of a given user
 */
export interface UserNotificationSettingsPermissions {

  /**
   * Can the authenticated user view the notification settings of a given user?
   */
  view?: boolean;
}
