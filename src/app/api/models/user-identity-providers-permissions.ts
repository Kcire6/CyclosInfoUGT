/* tslint:disable */

/**
 * Permissions over the identity provider links
 */
export interface UserIdentityProvidersPermissions {

  /**
   * Can view the relationships to identity providers?
   */
  view?: boolean;
}
