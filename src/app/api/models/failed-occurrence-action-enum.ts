/* tslint:disable */

/**
 * Possible actions that could be confirmed with a device for a recurring payment failed occurrence
 * Possible values are:
 * - `process`: Process the recurring payment failed occurrence
 */
export enum FailedOccurrenceActionEnum {
  PROCESS = 'process'
}
