/* tslint:disable */
import { BaseOrderAction } from './base-order-action';

/**
 * Parameters used to reject an order by the authenticated user.
 */
export interface RejectOrder extends BaseOrderAction {
}
