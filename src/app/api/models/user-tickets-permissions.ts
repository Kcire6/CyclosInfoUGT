/* tslint:disable */

/**
 * Permissions for tickets over a user
 */
export interface UserTicketsPermissions {

  /**
   * Can the authenticated administrator or broker view tickets of the given user?
   */
  view?: boolean;
}
