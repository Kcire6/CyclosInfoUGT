/* tslint:disable */
import { InternalNamedEntity } from './internal-named-entity';

/**
 * Represents an entity that is being referenced from another one, without caring about the type of the referenced entity.
 */
export interface EntityReference extends InternalNamedEntity {
}
