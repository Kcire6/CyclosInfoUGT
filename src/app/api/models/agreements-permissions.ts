/* tslint:disable */

/**
 * Permissions regarding the user agreements
 */
export interface AgreementsPermissions {

  /**
   * Can the authenticated user view the agreement log?
   */
  view?: boolean;
}
