/* tslint:disable */

/**
 * Permissions over notification settings
 */
export interface NotificationSettingsPermissions {

  /**
   * Whether the own notification settings are enabled or not.
   */
  enable?: boolean;
}
