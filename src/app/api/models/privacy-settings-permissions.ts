/* tslint:disable */

/**
 * Permissions regarding the privacy settings
 */
export interface PrivacySettingsPermissions {

  /**
   * Can the current user manage his own privacy settings?
   */
  manage?: boolean;

  /**
   * Can the current user view his own privacy settings?
   */
  view?: boolean;
}
